<h1><%= "#{@forecast.city.name} #{@forecast.date}" %></h1>

<%= link_to "New forecast", [:new, :forecast] %>
<p>
  <b>Temperature min:</b>
  <%= "#{@forecast.min_temperature} #{@forecast.unit}" %>
</p>

<p>
  <b>Temperature max:</b>
  <%= "#{@forecast.max_temperature} #{@forecast.unit}" %>
</p>

